// Pattern = Algorithm in the backend (same API, renamed for the UI)
export {
  AlgorithmService as PatternService,
  type Algorithm as Pattern,
  type AlgorithmFork as PatternFork,
  type AlgorithmProposal as PatternProposal,
} from './algorithmService';
