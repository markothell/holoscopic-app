{
  "name": "holoscopic",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:game": "npm run dev --workspace=apps/holoscopic-game",
    "dev:backend": "npm run dev --workspace=apps/backend",
    "dev:platform": "npm run dev --workspace=apps/platform",
    "build": "turbo build",
    "dev": "turbo dev"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
